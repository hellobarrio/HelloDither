export function EmptyHero() {
  return (
    <div className="empty-canvas-cta">
      <div className="empty-load-text">Load an image</div>
      <div className="sub">Drop a file anywhere on the canvas, or use the Upload button.</div>
    </div>
  );
}
